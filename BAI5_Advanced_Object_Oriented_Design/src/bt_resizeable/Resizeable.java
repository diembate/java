package bt_resizeable;

public interface Resizeable {

    //Sử dụng mã triển khai các lớp hình học ở repository này.
    // Hãy tạo một interface Resizeable với một phương thức void
    // có tên resize(double percent).
    // Phương thức này tăng kích thước các hình
    // lên tỷ lệ phần trăm được cho bởi đối số đầu vào.
    // Cho lớp Circle, Rectangle và Square triển khai phương thức này.
    //Bước 1: Viết mã kiểm thử cho interface Resizeable
    //
    //Bước 2: Xây dựng interface Resizeable với phương thức resize(double percent)
    //
    //Bước 3: Viết mã kiểm thử cho lớp Circle, Rectangle, Square triển khai interface Resizeable
    //
    //Bước 4: Viết mã triển khai interface Resizeable cho lớp Circle, Rectangle, Square
}
